# Disable directory browsing:
Options All -Indexes
# Prevent folder listing:
IndexIgnore *

<IfModule mod_rewrite.c>
RewriteEngine on

# Redirect Paths: EXAMPLE!
# RewriteRule ^(about|contact|this|that|search)/?$ index.php?p=$1

</IfModule>